# AI 系统协作规则

本文件给所有参与本项目的 AI 开发者使用。修改项目前必须先阅读 `README.md`、`ARCHITECTURE.md`、`AI_SYSTEM_PROMPT.md` 和 `DEVELOPMENT_LOG.md`。

## 项目原则

知识树画布不是列表式笔记应用。核心体验是画布中的知识节点和关系连线，节点详情只存简介和资源链接，知识正文由外部笔记、文档或资源承载。

必须保留两个视图原则：

- 横向视图只展示当前节点的前置节点、当前节点和后置节点。
- 纵向视图只展示当前节点的母节点、当前节点和子节点。

不要把默认画布改成整棵树全量展开，也不要把主要体验改成列表。

## 工程规则

1. 正式开发前必须确认四个基础文档存在：`README.md`、`ARCHITECTURE.md`、`AI_SYSTEM_PROMPT.md`、`DEVELOPMENT_LOG.md`。
2. 前端代码放在 `apps/web`。
3. 桌面应用壳放在 `apps/desktop`。
4. 可复用 schema、类型和校验逻辑放在 `packages/schema`。
5. 示例或可交换数据放在 `data/examples`。
6. 新增后端时放在 `apps/api`，不要混入前端或桌面目录。
7. 不做无关重构。每次改动围绕当前需求完成。

## 数据规则

- 知识树导入导出必须使用 `TreeDocument` JSON。
- 所有导入 JSON 必须通过 `packages/schema` 校验。
- 节点不要直接嵌套子节点数组。节点、边、资源和布局保持标准化存储。
- 修改 schema 时必须提升 `schemaVersion` 并提供迁移策略。
- 不要破坏已有用户本地 IndexedDB 数据。

## 交互规则

- 节点是小方块，节点之间通过连线表达关系。
- 鼠标滚轮在画布上缩放，拖动画布空白处平移。
- 拖动节点改变位置。
- 通过连线建立前置、后置、母子或相关关系。
- 单击节点进入该节点的纵向页面。
- 双击节点返回上一级画布视图。
- 右键树集显示复制、重命名、删除等操作。
- 右键节点可以删除当前节点和它的所有子节点。
- 右键资料链接可以删除该链接。

## 代码规则

- 使用 TypeScript，不新增未类型化的核心数据结构。
- UI 组件放在 `apps/web/src/components`。
- 状态写操作放在 `apps/web/src/store`。
- 纯计算和 IO 工具放在 `apps/web/src/lib`。
- Electron 主进程代码放在 `apps/desktop/src`。
- 样式放在 `apps/web/src/styles.css`，保持清楚、克制、工具化。
- 任何新功能都要考虑导入导出后的数据完整性。

## AI 接入规则

未来接入 AI 自动整理时，AI 只能产出结构化操作建议，不应直接改写 UI 状态。推荐操作格式包括创建节点、更新节点、创建关系、删除关系和移动节点。操作必须先校验、再预览、最后由用户确认执行。

## 日志规则

每次 AI 修改项目后，必须在 `DEVELOPMENT_LOG.md` 追加记录，包含：

- 日期。
- 修改摘要。
- 涉及文件。
- 验证方式。
- 后续风险或待办。

不要只在聊天里说明修改，必须写入日志文件。

## 验证规则

每次修改功能后，必须在交给用户测试之前完成与该功能直接相关的验证。

- 关系逻辑修改必须补充或运行图关系单元测试。
- UI 交互修改必须做浏览器端真实操作回归。
- 桌面应用相关修改必须至少构建安装包或启动未安装版桌面程序验证。
- 验证结果必须写入 `DEVELOPMENT_LOG.md`。
- 如果某项验证无法执行，必须在回复中明确说明原因和风险。
